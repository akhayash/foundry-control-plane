// Azure AI Foundry Control Plane - Configure Parameters Template
// ===============================================================
// Copy this file to dev.configure.bicepparam or prod.configure.bicepparam
// and fill in environment-specific values.
// NOTE: Get resource names from deployment output or Azure portal.

using '../configure/main.bicep'

// Environment: 'dev' | 'prod'
param environment = '<ENVIRONMENT>'

// Base name used during deployment
param baseName = '<BASE_NAME>'

// Override resource names if they differ from naming convention
// Get these from deployment output or Azure portal
param aiServicesNameOverride = '<AI_SERVICES_NAME>'  // e.g., 'aiffcpncdevpn3s'
param projectNameOverride = '<PROJECT_NAME>'         // e.g., 'aifpfcpndevpn3s'

// Application Insights configuration (optional)
param enableAppInsights = false
param appInsightsApiKey = ''  // If enableAppInsights=true, provide API key
