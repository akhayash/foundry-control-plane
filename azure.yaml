# Azure Developer CLI (azd) configuration
# https://learn.microsoft.com/azure/developer/azure-developer-cli/

name: foundry-control-plane
metadata:
  template: azd-init

# Infrastructure configuration
# deploy/ フォルダですべてのリソース作成と設定を実行
infra:
  provider: bicep
  path: infra/deploy
  module: main
